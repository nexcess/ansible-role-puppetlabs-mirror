---

puppetlabs_mirror_source: "rsync://rsync.puppet.com/packages/"
puppetlabs_mirror_cron_disabled: true
puppetlabs_mirror_cron_minute: "{{ 59 | random(seed=inventory_hostname) }}"
puppetlabs_mirror_cron_hour: "{{ 23 | random(seed=inventory_hostname) }}"
